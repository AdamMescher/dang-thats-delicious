mixin forgotForm()
    form.form(action="/account/forgot" method="POST")
        h2 🕵️ I forgot my password!
        label(for="email") 📧 Email
        input(type="email" name="email" placeholder="Email")
        input.button(type="submit" value="Send a Reset 📬")